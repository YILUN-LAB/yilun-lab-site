# Resend API key — used by the /contact form to send mail.
# Keep the actual key in .env.local (gitignored) and Vercel environment variables.
RESEND_API_KEY=

# Vercel supplies VERCEL_ENV automatically. To test preview metadata locally:
# VERCEL_ENV=preview npm run build
